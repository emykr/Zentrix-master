import React from 'react';

const ToolbarLeft: React.FC = () => {
  return (
    <div className="w-16 bg-slate-800 border-r border-slate-700">
      {/* 도구 버튼들은 나중에 추가 */}
    </div>
  );
};

export default ToolbarLeft;
